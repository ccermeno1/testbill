# The weights of the served runs travel through Git LFS: the repo keeps a
# pointer, the binaries go apart. `git lfs install` once per machine before
# adding or cloning them.
runs/**/weights/* filter=lfs diff=lfs merge=lfs -text
